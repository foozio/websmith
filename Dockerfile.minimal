# Minimal production Dockerfile for Websmith Kit Documentation
FROM nginx:alpine

# Copy the static export output
COPY apps/docs/out /usr/share/nginx/html

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]